# 产品概要设计：EvoGen（进化灵眸）——下一代个人 AI 智能体

---

## 文档信息

| 属性 | 内容 |
|------|------|
| **产品代号** | EvoGen（Evolutionary Generalist） |
| **中文名** | 进化灵眸 |
| **文档版本** | v2.1 |
| **作者** | 产品架构师 |
| **状态** | 已实现同步更新 |
| **前置文档** | 产品需求文档 v1.5（`01-产品需求文档-v1.4.md`） |
| **后置文档** | 产品详细设计 v2.1（`03-产品详细设计-v2.0.md`） |
| **基础代码库** | Fork Hermes Agent 运行时核心（MIT 协议） |
| **技术栈** | Python 3.12+ 后端 + React 18 前端 + Chroma 向量库（MVP）+ BGE-M3 Embedding（1024d） |
| **团队模型** | M0-M1 单人开发，M2+ 扩团队 |

### v2.1 修订说明（实现同步更新）

| 变更 | 说明 |
|------|------|
| **基座重构** | 从"自研对比 Hermes/OpenClaw"改为"Fork Hermes Agent + 增量开发"，第六章 MVP 收缩方案的 Hermes 已有资产作为设计起点 |
| **架构图重构** | 新增「继承层 + 增量层」双层架构视图，清晰标注哪些是零成本继承、哪些是 MVP 新增、哪些是远期目标 |
| **竞品矩阵重构** | L2 场景竞品从开发者工具（Claude Code/Codex CLI）替换为消费级 AI 助手（豆包/元宝/ChatGPT），并新增完整的差异化对比分析 |
| **定位对齐** | 核心定位从"开发者工具"调整为"个人 AI 智能体"，删除专业编码能力对标，新增日常陪伴 + 轻量创作场景 |
| **MVP 对齐** | 模块划分中明确标注 MVP 交付范围、M1/M2/M3 分期交付路线 |
| **Web 前端** | 新增前端路由与模块清单，BS 瘦客户端架构 |
| **对比删除** | 删除与 OpenClaw Canvas/Voice 等远期能力的对比（不属 MVP 范围） |
| **v2.1 新增** | 模块树新增：6.7 资源库模块（技能/工具/经验）、6.8 系统状态面板、6.9 联网搜索、6.10 制品面板；前端路由同步更新；Embedding 模型更新为 BGE-M3 1024d；M0 里程碑状态标记已交付 |

---

## 一、产品定位与愿景

### 1.1 一句话定位

> **EvoGen 是唯一一个你用得越久、它就越懂你的个人 AI 智能体。**

### 1.2 核心价值主张

```
        ┌─────────────────────────────────┐
        │       消费级 AI 助手              │
        │  (豆包 / 元宝 / ChatGPT)          │
        │  "每次都是新大脑"                  │
        │  无记忆 → 无成长 → 同样问题反复问   │
        └─────────────────────────────────┘
                       ↓ 进化
        ┌─────────────────────────────────┐
        │         EvoGen                   │
        │  "越用越懂你，越用越强"            │
        │  持久记忆 + 经验积累 + 统一人格     │
        └─────────────────────────────────┘
```

EvoGen 的独特价值不在编码能力（不标榜专业 IDE），而在 **「记忆 + 进化 + 人格」** 三位一体的陪伴式智能体验：

1. **三层进化记忆**：瞬态（会话内）→ 工作（跨会话项目上下文）→ 核心（持久化自我认知）。Agent 自动记住你的偏好、项目、习惯，下次对话无需重来。
2. **经验记录与反馈**：Agent 记录每次任务执行轨迹，你可以标注「这里做得好」「下次先 X 再 Y」，下次遇到相似场景自动提示。
3. **跨平台统一人格**：无论在 Telegram、飞书还是 Web 端，同一个 EvoGen 风格一致、记忆一致、价值观一致。

### 1.3 设计哲学

1. **记忆优先**（Memory-First）：一切能力以持久记忆为基础，记忆驱动上下文组装和决策。
2. **经验积累**（Experience-Accumulating）：MVP 阶段做轨迹记录 + 人工反馈，远期做自动优化。
3. **人格统一**（Unified Persona）：跨平台/跨会话保持一致的数字人格。
4. **渐进自主**（Progressive Autonomy）：从需要确认到自主执行，用户可控。
5. **本地优先**（Local-First）：记忆和知识图谱默认本地存储，用户拥有一切数据。

---

## 二、系统顶层架构

### 2.1 架构设计原则

EvoGen 的架构基于 **Fork Hermes Agent 运行时核心（MIT 协议）** 进行增量开发。整体策略：

- **继承层（零成本）**：Gateway、Agent Loop、工具系统、技能系统、飞书/CLI 通道、压缩、沙盒、Cron/Webhooks、MCP、Web 前端框架 —— 全部来自 Hermes Fork，直接复用。
- **增量层（MVP 新增）**：三层记忆引擎、经验记录系统、统一人格同步层、1 个新 IM 通道、记忆浏览器 UI。
- **远期层（M1-M3）**：知识图谱、GUI Agent、经验自动优化、技能市场、多 Agent 进化协作。

### 2.2 架构全景图

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           EvoGen 系统架构（v2.0 MVP）                          │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │                     📱 用户触点层                                  │       │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │       │
│  │  │ CLI 终端  │  │ Web 前端  │  │ 飞书 Bot  │  │ Telegram/Discord │  │       │
│  │  │ (完整功能)│  │ (管理面板)│  │ (消息通道)│  │ (新增 IM 通道)   │  │       │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────────┬─────────┘  │       │
│  └───────┼─────────────┼─────────────┼─────────────────┼────────────┘       │
│          │             │             │                 │                     │
│  ┌───────┴─────────────┴─────────────┴─────────────────┴────────────┐       │
│  │                 🔌 通道网关（Channel Gateway）  ← Hermes 继承      │       │
│  │  ┌──────────────────────────────────────────────────────────┐    │       │
│  │  │  WebSocket + JSON 协议层                                   │    │       │
│  │  │  · 连接管理  · 身份认证(Token/Pairing)  · 消息路由         │    │       │
│  │  │  · 事件推送  · 流式传输(SSE/Stream)  · 协议适配           │    │       │
│  │  └──────────────────────────────────────────────────────────┘    │       │
│  │  ┌──────────────────────────────────────────────────────────┐    │       │
│  │  │  平台适配器（Platform Adapters）                            │    │       │
│  │  │  飞书 ✅ | CLI ✅ | Telegram 🆕 | Discord 🆕 (二选一)      │    │       │
│  │  └──────────────────────────────────────────────────────────┘    │       │
│  └────────────────────────────────┬───────────────────────────────┘       │
│                                   │                                         │
│  ┌────────────────────────────────┴───────────────────────────────┐       │
│  │                    🧠 Agent 运行时核心  ← Hermes 继承            │       │
│  │                                                                 │       │
│  │  ┌────────────────────────────────────────────────────────┐    │       │
│  │  │  Agent Loop（主循环）  ← Hermes 继承                      │    │       │
│  │  │  · 接收输入 · 上下文组装 · LLM 调用 · 工具分发 · 响应输出  │    │       │
│  │  └────────────────────────────────────────────────────────┘    │       │
│  │                                                                 │       │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │       │
│  │  │  工具系统      │  │  技能引擎     │  │  会话管理            │  │       │
│  │  │  ← Hermes    │  │  ← Hermes    │  │  ← Hermes            │  │       │
│  │  │  工具注册/发现 │  │  技能加载/匹配│  │  会话 CRUD/搜索/压缩  │  │       │
│  │  │  命令审批/沙盒 │  │  本地管理     │  │  FTS5 全文检索       │  │       │
│  │  └──────────────┘  └──────────────┘  └──────────────────────┘  │       │
│  │                                                                 │       │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │       │
│  │  │  模型路由      │  │  沙盒执行     │  │  调度系统            │  │       │
│  │  │  ← Hermes    │  │  ← Hermes    │  │  ← Hermes            │  │       │
│  │  │  Provider Pool│  │  Docker/SSH  │  │  Cron + Webhooks     │  │       │
│  │  │  故障转移     │  │  /本地执行    │  │  Delegate/Kanban     │  │       │
│  │  └──────────────┘  └──────────────┘  └──────────────────────┘  │       │
│  └────────────────────────────────┬───────────────────────────────┘       │
│                                   │                                         │
│  ┌────────────────────────────────┴───────────────────────────────┐       │
│  │                ⭐ 进化层（Evolution Layer）—— MVP 核心新增       │       │
│  │                                                                 │       │
│  │  ┌──────────────────────────────────────────────────────────┐  │       │
│  │  │  🧬 进化记忆引擎（EvoMemory Engine）  🆕 MVP 核心           │  │       │
│  │  │                                                            │  │       │
│  │  │  ┌──────────┐    ┌──────────┐    ┌──────────┐             │  │       │
│  │  │  │ 瞬态记忆  │ →  │ 工作记忆  │ →  │ 核心记忆  │             │  │       │
│  │  │  │ (会话内)  │    │ (跨会话) │    │ (永久)   │             │  │       │
│  │  │  │ TTL:会话  │    │ TTL:衰减 │    │ TTL: ∞   │             │  │       │
│  │  │  └──────────┘    └──────────┘    └──────────┘             │  │       │
│  │  │       ↑               ↑               ↑                    │  │       │
│  │  │       └───────────────┴───────────────┘                    │  │       │
│  │  │              记忆管理器（Memory Manager）                    │  │       │
│  │  │    · 事实提取(LLM) · 向量检索(Chroma) · 衰减调度            │  │       │
│  │  │    · 冲突检测   · 合并去重    · 隐私分级                    │  │       │
│  │  │                                                            │  │       │
│  │  │  MVP 范围：工作记忆 + 手动保存 + 记忆浏览器 UI              │  │       │
│  │  │  M1 目标：自动提取 + 衰减模型 + 自动强化                    │  │       │
│  │  └──────────────────────────────────────────────────────────┘  │       │
│  │                                                                 │       │
│  │  ┌──────────────────────────────────────────────────────────┐  │       │
│  │  │  📝 经验记录引擎（Trace Recorder）  🆕 MVP                 │  │       │
│  │  │                                                            │  │       │
│  │  │  任务执行 → 轨迹记录(自动) → 用户反馈(手动标注)              │  │       │
│  │  │                                    ↓                       │  │       │
│  │  │                          场景关联提示(再次遇到相似场景)      │  │       │
│  │  │                                                            │  │       │
│  │  │  MVP 范围：日志级轨迹记录 + 手动反馈 + 场景关联             │  │       │
│  │  │  M2 目标：半自动经验提炼 → M3+：自动优化(研究阶段)           │  │       │
│  │  └──────────────────────────────────────────────────────────┘  │       │
│  │                                                                 │       │
│  │  ┌──────────────────────────────────────────────────────────┐  │       │
│  │  │  👤 统一人格引擎（Unified Persona）  🆕 MVP                │  │       │
│  │  │                                                            │  │       │
│  │  │  跨 Profile 同步层 + 基础用户属性存储                       │  │       │
│  │  │  · 称呼偏好  · 回复风格(简洁/详细)  · 沟通语气             │  │       │
│  │  │  · 跨平台一致注入 System Prompt                            │  │       │
│  │  │                                                            │  │       │
│  │  │  MVP 范围：跨 profile 同步 + 基础属性存储                   │  │       │
│  │  │  M1 目标：渐进式了解 + 动态调整                             │  │       │
│  │  └──────────────────────────────────────────────────────────┘  │       │
│  └────────────────────────────────┬───────────────────────────────┘       │
│                                   │                                         │
│  ┌────────────────────────────────┴───────────────────────────────┐       │
│  │                     💾 存储层（Storage Layer）                   │       │
│  │                                                                 │       │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐    │       │
│  │  │ SQLite + FTS5  │  │ Chroma (向量)   │  │ 文件系统        │    │       │
│  │  │ ← Hermes 继承   │  │ 🆕 MVP 新增     │  │ ← Hermes 继承   │    │       │
│  │  │ · 会话/消息     │  │ · 记忆嵌入存储  │  │ · 技能/SKILL.md │    │       │
│  │  │ · 配置/Token   │  │ · 语义相似检索  │  │ · 日志/轨迹     │    │       │
│  │  │ · 经验记录     │  │                 │  │ · 配置文件      │    │       │
│  │  └────────────────┘  └────────────────┘  └────────────────┘    │       │
│  │                                                                 │       │
│  │  M1 目标：pgvector 替代 Chroma（跟 PostgreSQL 统一运维）         │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│  图例：← Hermes 继承(零成本)  |  🆕 MVP 新增  |  🔮 M1-M3 远期               │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 2.3 核心架构决策记录（ADR）

| ID | 决策 | 理由 | 备选方案 |
|----|------|------|----------|
| ADR-001 | **Fork Hermes Agent 运行时核心**（MIT） | 成熟 Agent 运行时：Gateway/工具/技能/通道/压缩/沙盒/调度全就绪。MVP 只需增量开发记忆+经验+人格三个模块，归零基础设施成本 | 从零自研（9-14 人·月）；基于 agent-framework（需大量改造） |
| ADR-002 | **记忆引擎独立为单独模块** | 解耦 Agent 循环和记忆，便于独立演进。Hermes 现有 memory 工具（key-value）作为工作记忆存储层，在其上扩展三层 schema + 向量检索 | 内嵌在 Agent Loop 中（耦合度高，难以独立测试和优化） |
| ADR-003 | **Chroma 向量数据库（MVP）→ pgvector（生产）** | Chroma：`pip install chromadb` 零运维/零部署，适合单机原型验证。后期迁移 pgvector 与 PostgreSQL 统一运维 | Qdrant（需独立部署）；LanceDB（Rust 生态，Python 集成不如 Chroma 成熟） |
| ADR-004 | **经验学习 MVP 阶段仅做轨迹记录 + 人工反馈** | 自动经验优化涉及开放研究问题，MVP 不做。先完成数据采集管道，M2+ 再做自动提炼 | 直接做自动优化（风险极高，M0 不可行） |
| ADR-005 | **WebSocket + JSON 协议（继承 Hermes）** | Hermes 已验证的成熟方案，支持流式 + 双向通信。浏览器原生支持 | gRPC（性能好但 Web 需代理）；SSE（单向，不支持双向） |
| ADR-006 | **纯应用层，不微调模型** | 记忆提取/任务编排通过 prompt engineering 实现，保持模型无关。M1 后评估是否微调小模型做特定子任务 | 自研/微调专用模型（成本高、维护重、与基座模型迭代速度脱节） |

### 2.4 核心数据流概览

```
用户输入 (任意平台)
       │
       ↓
  ┌─────────┐     ┌──────────────┐     ┌─────────────┐
  │ Gateway  │ →   │ Agent Loop   │ →   │  LLM 调用   │
  │ 消息路由  │     │ 上下文组装    │     │  模型路由    │
  └─────────┘     └──────┬───────┘     └─────────────┘
                         │
          ┌──────────────┼──────────────┐
          ↓              ↓              ↓
  ┌──────────┐   ┌──────────┐   ┌──────────┐
  │ 记忆引擎  │   │ 人格引擎  │   │ 技能引擎  │
  │ 加载快照  │   │ 注入策略  │   │ 匹配技能  │
  └──────────┘   └──────────┘   └──────────┘
                         │
                         ↓
                  工具分发与执行
                         │
                         ↓
                    返回用户
                         │
          ┌──────────────┴──────────────┐
          ↓ (异步)                      ↓ (异步)
  ┌──────────────┐            ┌──────────────┐
  │ 记忆提取存储  │            │ 轨迹记录提交  │
  │ extract +    │            │ submit +     │
  │ store        │            │ scene-link   │
  └──────────────┘            └──────────────┘
```

---

## 三、模块划分

### 3.1 模块全景树（标注交付阶段）

```
EvoGen 模块体系
│
├── 1. Gateway 层（通道网关）← Hermes 继承
│   ├── 1.1 WebSocket 协议服务          [继承]
│   ├── 1.2 平台适配器                   [继承：飞书/CLI] + [🆕 MVP：1 个新 IM]
│   ├── 1.3 身份认证与配对               [继承]
│   └── 1.4 REST API 端点               [继承 + 🆕 MVP：记忆 API]
│
├── 2. Session 层（会话管理）← Hermes 继承
│   ├── 2.1 会话生命周期管理             [继承]
│   ├── 2.2 上下文引擎（组装/注入/压缩）   [继承 + 🆕 MVP：记忆感知压缩]
│   └── 2.3 会话持久化与 FTS5 搜索       [继承]
│
├── 3. Agent 层（智能体核心）← Hermes 继承
│   ├── 3.1 Agent Loop（主循环）         [继承 + 🆕 MVP：记忆/人格注入点]
│   ├── 3.2 工具链管理器                 [继承]
│   ├── 3.3 技能引擎                     [继承]
│   └── 3.4 模型路由 + Provider Pool     [继承]
│
├── 4. 进化层（核心差异化）🆕 全部 MVP 新增
│   ├── 4.1 进化记忆引擎（EvoMemory）    [🆕 MVP]
│   │   ├── 三层记忆存储（瞬态/工作/核心）
│   │   ├── 事实提取器（LLM zero-shot prompt）
│   │   ├── 向量检索引擎（Chroma）
│   │   ├── 记忆衰减调度器              [🔮 M1]
│   │   └── 冲突检测与合并
│   │
│   ├── 4.2 经验记录引擎（Trace Recorder）[🆕 MVP]
│   │   ├── 任务轨迹自动记录（日志级）
│   │   ├── 用户反馈标注接口
│   │   ├── 场景关联匹配
│   │   └── 经验检索与提示
│   │
│   └── 4.3 统一人格引擎（Unified Persona）[🆕 MVP]
│       ├── 基础用户属性存储
│       ├── 跨 Profile 同步层
│       └── System Prompt 注入管线
│
├── 5. 调度层 ← Hermes 继承
│   ├── 5.1 Cron 调度器                  [继承]
│   ├── 5.2 Webhook 触发器               [继承]
│   └── 5.3 Delegate/Kanban 协作         [继承，MVP 不重点使用]
│
├── 6. Web 前端（管理面板）← Hermes 继承框架 + 🆕 MVP 新增 ✅ 已实现
│   ├── 6.1 对话界面（Chat View）         [继承 + 改造] ✅
│   ├── 6.2 记忆浏览器（Memory Browser）   [🆕 MVP] ✅
│   ├── 6.3 经验面板（Experience Panel）   [🆕 MVP] ✅
│   ├── 6.4 人格配置（Persona Config）     [🆕 MVP] ✅
│   ├── 6.5 设置面板（Settings）          [继承 + 改造] ✅
│   ├── 6.6 技能管理（Skills Manager）     [继承] ✅
│   ├── 6.7 资源库模块（Resource Hub）    [🆕 MVP] ✅
│   │   ├── 技能浏览/详情/导入导出
│   │   ├── 工具注册浏览（分类/标签/状态）
│   │   └── 经验轨迹管理（列表/详情/反馈）
│   ├── 6.8 系统状态面板（System Status）  [🆕 MVP] ✅
│   │   ├── Gateway 心跳 + PID 解析
│   │   ├── Agent 在线状态 + 运行时间
│   │   ├── 数据库连接 + 记忆统计
│   │   ├── 存储用量仪表盘（进度条 + 三色阈值）
│   │   └── 实时日志查看器（级别过滤/关键词搜索）
│   ├── 6.9 联网搜索（Web Search）        [🆕 MVP] ✅
│   │   ├── Tavily 搜索 API 集成
│   │   └── Jina Reader 网页全文抓取
│   └── 6.10 制品面板（Artifact Panel）   [🆕 MVP] ✅
│       ├── Code/Image/Doc 三 Tab 分类
│       ├── 语法高亮（20+ 语言）
│       └── 可折叠侧面板
│
├── 7. 生态层 ← Hermes 继承
│   ├── 7.1 MCP 协议适配                 [继承]
│   ├── 7.2 插件系统                     [继承]
│   └── 7.3 技能市场（EvoHub）           [🔮 M1-M2]
│
└── 8. 存储层
    ├── 8.1 SQLite + FTS5（会话/配置/日志）[继承]
    ├── 8.2 Chroma 向量存储（记忆嵌入）    [🆕 MVP]
    └── 8.3 文件系统（技能/轨迹/配置）      [继承]
```

### 3.2 MVP 交付范围总览

```
MVP 范围（Phase 0-1 已交付）：

继承（零成本，直接可用）：
✅ WebSocket Gateway + Agent Loop       ✅ 工具系统（注册/审批/沙盒）
✅ 飞书通道（消息收发/Markdown）          ✅ CLI 全功能
✅ Skills 本地管理                       ✅ Memory 基础层（key-value）
✅ Session Search（FTS5 全文检索）       ✅ Compaction（上下文压缩）
✅ Delegate / Kanban（多 Agent 协作）    ✅ Cron / Webhooks
✅ MCP 工具生态                           ✅ Web 前端框架（React）

Phase 0-1 已实现：
✅ F-001-Lite：三层记忆架构
   └─ BGE-M3 1024d 嵌入 + Chroma 向量检索 + 记忆浏览器 UI

✅ F-002-Lite：经验记录基础版
   └─ 日志级轨迹记录 + 手动反馈标注 + 场景关联提示

✅ F-003-Lite：统一人格
   └─ 跨 profile 同步 + 基础属性存储 + System Prompt 注入

✅ F-010 增强：上下文智能压缩
   └─ 记忆感知的压缩锚点 + 严格上下文组装顺序

✅ F-023：记忆容量管理
   └─ 容量上限配置 + 按年龄/重要性自动归档 + 存储用量仪表盘

✅ F-024：资源库模块
   └─ 侧边栏"资源库"分组：技能/工具/经验 统一管理

✅ F-025：系统状态面板
   └─ Gateway 心跳 + Agent 状态 + DB 状态 + 日志查看器 + 存储用量

✅ F-026：联网搜索
   └─ Tavily 搜索 + Jina Reader 网页抓取，对话中自动触发

✅ F-027：制品面板
   └─ 代码/图像/文档 可折叠预览，语法高亮

✅ 汉化全覆盖
   └─ 前端所有 UI 文字中文化

待完成：
🆕 F-004-Lite：新增 1 个 IM 通道
   └─ Telegram 或 Discord（复用 adapter 模式）
```

---

## 四、与竞品的差异化对比

### 4.1 竞品分层（三层竞争格局）

EvoGen 面对的不是一个对手，而是三层不同类型的竞争者：

| 层级 | 竞品类型 | 代表产品 | 竞争关系 |
|------|----------|----------|:---:|
| **L1 - 直接竞品** | 个人 AI Agent 框架/平台 | Hermes Agent、OpenClaw | 🔴 同一赛道 |
| **L2 - 场景竞品** | 消费级 AI 助手 | 豆包、元宝、ChatGPT | 🟠 抢夺日常使用场景和大众用户心智 |
| **L3 - 潜在竞品** | 模型层向下延伸 | OpenAI/Anthropic 在 API 层加入持久记忆 | 🟡 长期威胁（18-24 个月） |

### 4.2 L1 直接竞品对比（Agent 框架）

| 维度 | Hermes Agent | OpenClaw | **EvoGen（基于 Hermes Fork）** |
|------|:---:|:---:|:---:|
| **定位** | 开发者 Agent 框架 | 开发者 Agent 框架 | **个人 AI 智能体**（日常陪伴 + 轻量创作） |
| **持久记忆** | 静态 key-value（手动） | 静态 key-value（手动） | **三层进化记忆 + 向量检索 + 记忆浏览器 UI** |
| **学习能力** | Skills 手动保存 | Skills 手动安装 | **经验记录 + 人工反馈 + 场景关联提示** |
| **人格系统** | 按 Profile 隔离 | SOUL.md 文件 | **跨平台统一人格 + 自动注入 System Prompt** |
| **目标用户** | 开发者 | 开发者 | **数字原住民 / 创意工作者 / 普通用户** |
| **安装体验** | CLI 安装后需配置 | CLI 安装后需配置 | **继承 Hermes 一键安装 + 渐进式引导** |
| **开源协议** | MIT | 非 MIT（限制性） | **MIT**（继承 Hermes 协议，最宽松） |
| **底层关系** | EvoGen 的 Fork 源头 | — | **Fork Hermes，在 Agent 框架之上做消费级体验** |

> **核心判断**：EvoGen 不试图在 Agent 框架能力上与 Hermes/OpenClaw 竞争（本来就是 Fork Hermes），而是在**用户体验层**做差异化——让 Agent 框架「能用」变成「好用」，让普通用户也能享受 AI Agent 的能力。

### 4.3 L2 场景竞品对比（消费级 AI 助手）

| 维度 | 豆包 | 元宝 | ChatGPT | **EvoGen** |
|------|:---:|:---:|:---:|:---:|
| **持久记忆** | ❌ 无跨会话记忆 | ❌ 无跨会话记忆 | ⚠️ 有限（ChatGPT Memory，手动管理） | ✅ 三层自动记忆 + 向量检索 |
| **自我进化** | ❌ 每次从零开始 | ❌ 每次从零开始 | ❌ 静态能力 | ✅ 经验记录 + 反馈闭环 |
| **跨平台一致性** | ⚠️ App/Web/PC 割裂 | ⚠️ 仅在微信生态内 | ⚠️ App/Web 基本一致 | ✅ 统一人格 + 跨平台记忆同步 |
| **开源/自部署** | ❌ 完全闭源 | ❌ 完全闭源 | ❌ 完全闭源 | ✅ MIT 开源，可自部署 |
| **数据隐私** | ⚠️ 数据上云 | ⚠️ 数据上云 | ⚠️ 数据上云 | ✅ 本地优先，记忆默认本地存储 |
| **工具调用** | ⚠️ 有限插件 | ❌ 仅对话 | ✅ 基础工具 | ✅ 完整 Agent 工具链（继承 Hermes） |
| **任务自动化** | ❌ 仅对话 | ❌ 仅对话 | ⚠️ 有限（Tasks） | ✅ Cron/Webhook/Delegate 全链路 |
| **多平台通道** | ⚠️ App/Web/PC | ⚠️ App(微信) | ⚠️ App/Web/macOS | ✅ Telegram/飞书/CLI/Web 统一 |
| **用户画像** | 大众消费者 | 微信用户 | 全球通用 | **注重隐私+个性化的进阶用户** |

> **核心判断**：豆包/元宝/ChatGPT 的优势在「开箱即用」和「多模态能力」，劣势在「无记忆/无进化/数据不自主」。EvoGen 不与它们比「回答流畅度」或「多模态」，而比「越用越懂你」和「数据完全自己掌控」。EvoGen 的目标用户是：**受够了每次重来解释、想要一个真正记得自己的 AI、同时在意数据隐私的用户**。

### 4.4 差异化总结——EvoGen 的核心护城河

```
┌──────────────────────────────────────────────────────────────┐
│                    核心差异化三角                              │
│                                                              │
│                    ┌─────────────┐                           │
│                   ╱               ╲                          │
│                  ╱   进化记忆      ╲                         │
│                 ╱   三层架构       ╲                        │
│                ╱   向量检索        ╲                       │
│               ╱   记忆浏览器       ╲                      │
│              ╱   (数据护城河)       ╲                     │
│             ╱                       ╲                    │
│            ╱        EVOGEN           ╲                   │
│           ╱          灵魂             ╲                  │
│          ╱                            ╲                 │
│         ╱  经验记录  ───────  统一人格 ╲                │
│        ╱   轨迹+反馈       │   跨平台一致 ╲               │
│       ╱   (体验壁垒)       │   (生态壁垒)  ╲              │
│      ╱                     │                  ╲             │
│     ╱                      │                   ╲            │
│    ╱              开源 + 本地优先                    ╲       │
│   ╱              MIT / 自部署 / 数据自有              ╲      │
│  ╱─────────────────────────────────────────────────────╲     │
│                                                              │
│  基于 Hermes Agent Fork（MIT），继承成熟 Agent 运行时         │
│  注入「记忆 + 经验 + 人格」作为消费级差异化灵魂               │
└──────────────────────────────────────────────────────────────┘
```

### 4.5 护城河分析

| 差异化能力 | 竞品追赶难度 | 护城河类型 | 说明 |
|-----------|:---:|------|------|
| **进化记忆引擎** | 🟡 中高 | 数据网络效应 | 用户的记忆数据越多，迁移成本越高。竞品可复制技术架构，但复制不了已积累的记忆数据 |
| **经验记录与反馈** | 🟢 低（MVP版）→ 🟡 中（完整版） | 体验壁垒 | MVP 版技术门槛低，但闭环体验需要打磨。完整版（自动优化）涉及开放研究问题 |
| **统一人格系统** | 🟢 低 | 体验壁垒 | 技术上易模仿，但「人格一致性」体验打磨和记忆绑定需要时间 |
| **开源 + 自部署双模式** | 🟡 中 | 商业模式壁垒 | MIT 开源获取社区信任，自部署满足隐私需求——消费级竞品（豆包/元宝/ChatGPT）都是闭源 |
| **Hermes Fork 继承** | 🟢 低 | 先发优势 | 任何团队都可以 Fork Hermes，但 EvoGen 先做了消费级包装 |

---

## 五、技术选型建议

### 5.1 后端技术栈

| 组件 | MVP 选型 | 生产目标 | 理由 |
|------|----------|----------|------|
| **运行时** | Fork Hermes Agent（Python 3.12+） | 同左 | 成熟 Agent 运行时，Gateway/工具/通道/技能/调度全就绪 |
| **LLM 客户端** | LiteLLM（集成在 Hermes 中） | 同左 | 统一 100+ 提供商接口，模型切换零成本 |
| **向量数据库** | **Chroma**（`pip install chromadb`） | pgvector | MVP 零运维零部署；生产阶段与 PostgreSQL 统一 |
| **关系数据库** | SQLite + FTS5（继承 Hermes） | PostgreSQL | MVP 零配置；生产阶段需多用户/高并发支持 |
| **消息协议** | WebSocket + JSON（继承 Hermes） | 同左 | 生产验证，浏览器原生支持，双向流式 |
| **异步框架** | asyncio + websockets（继承 Hermes） | 同左 | Hermes 已验证的高并发方案 |
| **Embedding 模型** | BAAI/bge-m3（1024d）本地 CPT-BGE | 零 API 成本，1024 维中文最优，pip install 即用 |
| **沙盒** | Docker SDK + 本地进程（继承 Hermes） | 同左 | Docker 隔离 + 本地高性能 |
| **配置管理** | YAML + .env（继承 Hermes） | 同左 | 成熟方案，用户熟悉 |

### 5.2 前端技术栈

| 组件 | 选型 | 理由 |
|------|------|------|
| **框架** | **React 18** + TypeScript | 社区最成熟，Hermes 已有 Web 前端框架可复用 |
| **构建工具** | Vite | 快速开发服务器 + HMR |
| **样式** | Tailwind CSS | 原子化 CSS，快速迭代 UI |
| **状态管理** | React Context + useReducer | MVP 规模适中，无需引入 Redux |
| **路由** | React Router 6 | SPA 路由标准方案 |
| **HTTP 客户端** | fetch + WebSocket 原生 | BS 瘦客户端架构，无需 Axios 等重量库 |
| **组件库** | shadcn/ui（可选） | 基于 Radix，无样式冲突，按需引入 |

### 5.3 前端页面路由与模块清单

```
EvoGen Web 前端路由结构（v2.1 — Phase 0-1 已实现）

/                       → 重定向到 /chat
/chat                   → 对话主界面（Chat View）← 继承改造 ✅
/chat/:sessionId        → 指定会话对话界面

// 知识库
/memory                 → 记忆浏览器（Memory Browser）🆕 MVP ✅
├── /memory/list        → 记忆列表（全部/工作/核心/archive）
├── /memory/:factId     → 记忆详情/编辑
└── /memory/search      → 记忆搜索

// 经验
/experience             → 经验面板（Experience Panel）🆕 MVP ✅
├── /experience/list    → 经验记录列表
├── /experience/:expId  → 经验详情/反馈标注
└── /experience/feedback → 待反馈队列

// 人格
/persona                → 人格配置（Persona Config）🆕 MVP ✅
├── /persona/attributes → 基础属性编辑（称呼/风格/语气）
├── /persona/preferences → 偏好管理
└── /persona/sync       → 跨平台同步状态

// 资源库 🆕
/skills                 → 技能管理 ✅
├── /skills/local       → 本地技能浏览/创建/导入导出
├── /skills/local/:skillId → 技能详情
└── /skills/hub         → 技能市场（M1）
/tools                  → 工具注册浏览（分类/标签/状态）✅

// 系统
/settings               → 设置面板（继承改造）✅
├── /settings/models    → 模型配置
├── /settings/platforms → 平台通道管理
└── /settings/system    → 系统状态面板 ✅
```

### 5.4 前端组件树

```
App
├── Layout
│   ├── Sidebar
│   │   ├── SessionList          → 会话列表（继承）
│   │   ├── NavMenu              → 导航菜单
│   │   │   ├── ChatLink
│   │   │   ├── MemoryLink       🆕
│   │   │   ├── ExperienceLink   🆕
│   │   │   ├── PersonaLink      🆕
│   │   │   ├── SkillsLink
│   │   │   └── SettingsLink
│   │   └── UserInfo             → 用户/Profile 信息
│   │
│   └── MainContent
│       ├── ChatView             → 对话主界面（继承改造）
│       │   ├── MessageList      → 消息列表
│       │   ├── MessageBubble    → 单条消息（用户/助手/工具）
│       │   ├── ChatInput        → 输入框 + 发送
│       │   ├── ToolCallCard     → 工具调用卡片
│       │   └── StreamIndicator  → 流式响应指示器
│       │
│       ├── MemoryBrowser        → 记忆浏览器 🆕
│       │   ├── MemoryStats      → 记忆统计（总量/分层/衰减）
│       │   ├── MemoryList       → 记忆列表
│       │   ├── MemoryCard       → 单条记忆卡片
│       │   ├── MemoryEditor     → 记忆编辑弹窗
│       │   ├── MemorySearch     → 记忆搜索栏
│       │   └── LayerFilter      → 分层筛选（瞬态/工作/核心）
│       │
│       ├── ExperiencePanel      → 经验面板 🆕
│       │   ├── TraceList        → 任务轨迹列表
│       │   ├── TraceDetail      → 单条轨迹详情
│       │   ├── FeedbackForm     → 反馈标注表单
│       │   └── SceneLinkList    → 场景关联列表
│       │
│       ├── PersonaConfig        → 人格配置 🆕
│       │   ├── AttributeForm    → 属性编辑表单
│       │   ├── PreferenceList   → 偏好列表
│       │   ├── TonePreview      → 语气预览
│       │   └── SyncStatus       → 同步状态指示
│       │
│       ├── SkillsManager        → 技能管理（继承）✅
│       │   ├── SkillCard        → 技能卡片
│       │   └── SkillDetail      → 技能详情
│       │
│       ├── ToolsBrowser         → 工具浏览器 🆕 ✅
│       │   ├── ToolCard         → 工具卡片（名称/分类/状态）
│       │   └── ToolSearch       → 工具搜索/过滤
│       │
│       ├── SettingsPanel        → 设置面板（继承改造）✅
│       │   ├── ModelConfig      → 模型配置
│       │   ├── PlatformConfig   → 平台通道
│       │   ├── SystemStatus     → 系统状态（Agent/Gateway/DB/容量/日志）✅
│       │   └── StorageCapacity  → 存储用量仪表盘 🆕 ✅
│       │
│       └── ArtifactPanel        → 制品面板 🆕 ✅
│           ├── CodeTab          → 代码 Tab（语法高亮）
│           ├── ImageTab         → 图像 Tab
│           └── DocTab           → 文档 Tab
```

---

## 六、风险与缓解

| # | 风险 | 概率 | 影响 | 缓解措施 |
|---|------|:---:|:---:|------|
| R1 | **F-001/F-002 技术可行性未验证**——三层记忆和轨迹记录与 Hermes 现有架构的整合可能存在未知摩擦 | 中 | MVP 延期 | M0 Week 1 做技术 Spike 验证；若遇阻，进一步缩小 MVP 范围（如仅做双层记忆，放弃核心记忆层） |
| R2 | **MVP 排期乐观偏差**——单人 2.0-2.5 月包含 5 个新模块，可能不足 | 中 | 排期不准 | 严格遵守 MVP 收缩范围（不做自动提取、不做衰减、不做自动优化）。每两周评估进度，必要时砍功能保核心 |
| R3 | **竞品 Hermes/OpenClaw 快速追平记忆能力** | 高 | 差异化窗口收窄 | 重点投入「经验记录与反馈」——Hermes/OpenClaw 目前完全没有这个能力；同时强调「消费级体验」差异化而非技术能力 |
| R4 | **LLM 基座模型原生支持持久记忆**——如 OpenAI/Anthropic 在 API 层加入记忆功能 | 中高 | 我们的记忆层被覆盖 | 架构保持「模型无关」——记忆层在 Agent 编排层而非模型层；如果模型提供原生记忆，我们做「记忆增强」而非「记忆替代」 |
| R5 | **冷启动困难**——前 20 个内测用户获取难 | 中低 | 反馈样本不足 | 飞书定向邀请 10-20 人（已有通道）；即刻/V2EX/少数派 30-50 人（M1 扩量） |
| R6 | **记忆系统的隐私质疑**——用户担心「Agent 记住了太多」 | 中 | 信任危机 | 所有数据默认本地存储；「记忆浏览器」让用户随时查看/编辑/删除；MIT 开源代码可供审查 |
| R7 | **Chroma 向量库生产就绪度**——单机够用但可能遇到扩展瓶颈 | 低 | M1 迁移成本 | MVP 阶段单机足够；M1 评估迁移 pgvector，架构已预留向量存储抽象层 |

---

## 七、里程碑与交付节奏

| 阶段 | 时间 | 交付 | 团队 |
|------|------|------|:---:|
| **M0 - MVP 原型** | 第 1-2.5 月 | ✅ Phase 0-1 已交付：三层记忆 + 经验记录 + 统一人格 + 容量管理 + 资源库 + 系统状态 + 联网搜索 + 制品面板 + 汉化全覆盖。F-004（IM 通道）待完成 | 1 人 |
| **M1 - Alpha** | 第 2.5-6 月 | 记忆衰减模型 + 自动提取 + 经验反馈闭环 + 技能市场 v1 + 3+ 平台 | 1-2 人 |
| **M2 - Beta** | 第 6-9 月 | 知识图谱 + GUI Agent POC + 社区技能 + 开源发布 + 英文版 | 2-3 人 |
| **M3 - 1.0 正式版** | 第 9-13 月 | P0+P1 完整版 + 企业版 + Pro 订阅上线 | 3-5 人 |

---

*本文档与 `03-产品详细设计-v2.0.md` 配套阅读。概要设计回答「模块在哪、怎么分、为什么这么选」，详细设计回答「每个模块怎么做、接口长什么样」。*
