# 前端需求规格说明书

---

## 文档信息

| 属性 | 内容 |
|------|------|
| **产品代号** | EvoGen（进化灵眸） |
| **文档版本** | v1.0 |
| **创建日期** | 2026-05-24 |
| **状态** | 待完善 |
| **技术栈** | React 18 + TypeScript + Tailwind CSS v4.3.0 |

---

## 一、当前前端代码状态分析

### 1.1 代码位置

当前前端代码**仅包含编译后的 dist 文件**，无前端源码目录 `frontend/src/`：

```
frontend/
├── dist/
│   ├── assets/
│   │   ├── index-CsNObq8S.css  (Tailwind CSS v4.3.0)
│   │   └── index-Dyety0Ut.js    (React 18 编译产物)
│   ├── favicon.svg
│   └── index.html
```

### 1.2 已确认实现的组件/类名

| 类名/组件 | 位置 | 说明 |
|-----------|------|------|
| `theme-toggle` | index-Dyety0Ut.js:285 | 日间/夜间模式切换按钮 |
| `evogen-theme` | index-Dyety0Ut.js:285 | 主题状态存储 key |
| `dark` | index-Dyety0Ut.js:285 | 深色模式 class 标识 |

### 1.3 已确认未实现的类名/效果

通过搜索 `dist/assets/` 下的编译文件，以下类名**均未找到**：

- `scanline` - 扫描线效果
- `glass` - 液态玻璃质感
- `shadow-glow` / `shadow-glow-coral` - 霓虹光晕
- `neon` - 霓虹脉冲发光
- `particle` - 网格粒子动效
- `gradient` - 渐变文字
- `carousel` - 轮播组件

---

## 二、着陆页需求

### 2.1 背景效果

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 网格粒子动效 | 40px 网格 + 径向遮罩 | ❌ 未实现 |
| 扫描线效果 | `.scanline` 持续运行动画 | ❌ 未实现 |
| 深空渐变背景 | 不要纯黑底色，需深空渐变 | ❌ 未实现 |

### 2.2 "进入 EvoGen" 按钮

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 霓虹脉冲发光动画 | 按钮需要有脉冲发光效果 | ❌ 未实现 |
| 液态玻璃质感 | 玻璃态按钮质感 | ❌ 未实现 |
| 悬浮发光阴影 | 鼠标悬浮时发光阴影 | ❌ 未实现 |

### 2.3 核心能力轮播

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 5 项核心能力轮播展示 | 展示 EvoGen 5 项核心能力 | ❌ 未实现 |

### 2.4 品牌标识

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 渐变文字 EvoGen 标识 | 珊瑚→全息银渐变文字 | ❌ 未实现 |
| "进化灵眸" 副标题 | 品牌副标题 | ❌ 未实现 |

---

## 三、对话界面等内部页面需求

### 3.1 背景效果

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| body::before 40px 网格 | CSS 伪元素实现 40px 网格背景 | ❌ 未实现 |
| 径向遮罩 | 网格中央径向渐变遮罩 | ❌ 未实现 |
| 扫描线效果 | `.scanline` 持续运行 | ❌ 未实现 |

### 3.2 液态玻璃卡片

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| `.glass` 类名 | 毛玻璃 blur(24px) + saturate(180%) | ❌ 未实现 |
| 可被 document.querySelector 搜索到 | 浏览器 Elements 中可搜到 | ❌ 未实现 |

### 3.3 卡片悬浮效果

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| border-color 切换 | 悬浮时边框颜色变化 | ❌ 未实现 |
| shadow-glow-coral 霓虹光晕 | 悬浮时珊瑚色霓虹阴影 | ❌ 未实现 |

### 3.4 按钮效果

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| cubic-bezier(0.34, 1.56, 0.64, 1) | 弹簧曲线弹跳动画 | ❌ 未实现 |

### 3.5 渐变文字

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 珊瑚→全息银渐变 | 特定渐变色值 | ❌ 未实现 |

### 3.6 旋转边框

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| conic-gradient 动画 | 边框旋转动画 | ❌ 未实现 |

---

## 四、布局结构需求

### 4.1 DeerFlow 三栏式布局

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 左侧边栏 | 固定宽度 260px | ❌ 未实现 |
| 中央对话区 | flex-1 自适应宽度 | ❌ 未实现 |
| 右侧制品面板 | 固定宽度 380px | ❌ 未实现 |

### 4.2 LightRAG 标签页式布局

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 标签页式模块切换 | Tab 切换不同功能模块 | ❌ 未实现 |

### 4.3 右侧制品面板

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 默认折叠 | 首次加载时折叠状态 | ❌ 未实现 |

---

## 五、配色风格需求

### 5.1 赛博朋克/超色彩主义

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 深色模式打底 | 主背景为深色 | ✅ 已实现（theme-toggle 确认） |
| 电子霓虹色点缀 | 界面点缀霓虹色 | ❌ 未实现 |
| 日落珊瑚色 | 特定强调色 | ❌ 未实现 |
| 全息银色 | 特定渐变色 | ❌ 未实现 |

### 5.2 日间/夜间模式

| 需求项 | 要求描述 | 状态 |
|--------|----------|:----:|
| 日间/夜间模式切换 | 切换按钮正常工作 | ✅ 已实现（theme-toggle 确认） |
| 切换动画流畅 | 切换有平滑过渡 | ❌ 未验证 |

---

## 六、类名可搜索性需求

### 6.1 必需可搜索的类名

| 类名 | 要求 | 状态 |
|------|------|:----:|
| `.scanline` | document.querySelector('.scanline') 必须返回元素 | ❌ 未实现 |
| `.glass` | 液态玻璃卡片类名 | ❌ 未实现 |
| `.theme-toggle` | 主题切换按钮 | ✅ 已实现 |

### 6.2 特效类名清单

以下类名**全部未实现**，需要在开发时添加：

| 类名 | 用途 |
|------|------|
| `scanline` | 扫描线动效 |
| `glass` | 液态玻璃卡片 |
| `shadow-glow-coral` | 珊瑚色霓虹光晕 |
| `neon-pulse` | 霓虹脉冲动画 |
| `gradient-text` | 渐变文字 |
| `particle-grid` | 网格粒子背景 |
| `rotating-border` | 旋转边框 |
| `spring-bounce` | 弹簧弹跳动画 |

---

## 七、已完成功能清单

| 功能 | 说明 |
|------|------|
| 日间/夜间模式切换 | theme-toggle 按钮已实现，localStorage 存储主题状态 |

---

## 八、待完善功能清单

### 8.1 优先级 P0（必须实现）

1. **背景特效系统**
   - 网格粒子动效实现（40px 网格 + 径向遮罩）
   - 扫描线效果 `.scanline` 类名 + 持续动画
   - 深空渐变背景（非纯黑）

2. **液态玻璃卡片系统**
   - `.glass` 类名实现（blur(24px) + saturate(180%)）
   - 卡片悬浮效果（border-color + shadow-glow-coral）

3. **着陆页 "进入 EvoGen" 按钮**
   - 霓虹脉冲发光动画
   - 液态玻璃质感
   - 悬浮发光阴影

4. **类名可搜索性**
   - 确保 `document.querySelector('.scanline')` 返回元素
   - 所有特效类名在 Elements 中可搜到

### 8.2 优先级 P1（重要）

5. **DeerFlow 三栏式布局**
   - 左侧边栏 260px
   - 中央对话区 flex-1
   - 右侧制品面板 380px
   - 右侧面板默认折叠

6. **品牌标识**
   - 渐变文字 EvoGen 标识（珊瑚→全息银）
   - "进化灵眸" 副标题

7. **按钮弹簧动画**
   - cubic-bezier(0.34, 1.56, 0.64, 1) 弹簧曲线

### 8.3 优先级 P2（增强）

8. **5 项核心能力轮播展示**
   - 轮播组件实现

9. **LightRAG 标签页式布局**
   - Tab 切换模块

10. **旋转边框效果**
    - conic-gradient 动画

---

## 九、检测验收条件

### 9.1 必需通过的检测

```javascript
// 检测 .scanline 是否存在
document.querySelector('.scanline')  // 必须返回 HTML 元素

// 检测 .glass 是否存在
document.querySelector('.glass')     // 必须返回 HTML 元素

// 检测 .theme-toggle 是否存在
document.querySelector('.theme-toggle')  // 已确认返回按钮元素
```

### 9.2 浏览器控制台验证

| 检测语句 | 预期结果 |
|----------|----------|
| `document.querySelector('.scanline')` | 返回 Element（不为 null） |
| `document.querySelector('.glass')` | 返回 Element（不为 null） |
| `document.querySelector('.theme-toggle')` | 返回 Element |

---

## 十、文件结构要求

建议的前端源码目录结构：

```
frontend/src/
├── components/
│   ├── landing/
│   │   ├── LandingPage.tsx
│   │   ├── ParticleGrid.tsx
│   │   ├── Scanline.tsx
│   │   ├── GradientTitle.tsx
│   │   └── NeonButton.tsx
│   ├── layout/
│   │   ├── DeerFlowLayout.tsx
│   │   ├── Sidebar.tsx
│   │   ├── ChatArea.tsx
│   │   └── ArtifactPanel.tsx
│   ├── chat/
│   │   ├── ChatView.tsx
│   │   ├── MessageList.tsx
│   │   └── ChatInput.tsx
│   ├── glass/
│   │   ├── GlassCard.tsx
│   │   └── GlassButton.tsx
│   └── common/
│       ├── ThemeToggle.tsx
│       └── GradientText.tsx
├── styles/
│   └── effects.css          # 特效样式（scanline, glass, neon 等）
├── App.tsx
└── main.tsx
```

---

## 十一、后续行动

1. **创建前端源码目录** `frontend/src/`
2. **实现背景特效系统**（粒子网格 + 扫描线）
3. **实现液态玻璃组件库**（GlassCard、GlassButton）
4. **实现着陆页完整 UI**
5. **实现 DeerFlow 三栏布局**
6. **验证所有类名可搜索性**

---

*文档生成时间：2026-05-24*
*依据：用户需求清单 + dist 文件分析*
