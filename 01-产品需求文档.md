# 产品需求文档（PRD）：EvoGen（进化灵眸）——下一代个人 AI 智能体

---

## 文档信息

| 属性 | 内容 |
|------|------|
| **产品代号** | EvoGen（Evolutionary Generalist） |
| **中文名** | 进化灵眸 |
| **文档版本** | v1.0 |
| **作者** | 产品架构师 |
| **状态** | 初稿 / 待评审 |
| **对标竞品** | Hermes Agent、OpenClaw、Claude Code、Codex CLI |

---

## 一、市场定位与目标用户

### 1.1 产品愿景

EvoGen 是一款**会成长的个人 AI 智能体**——它不只是一个工具，而是一个会**记住你、理解你、从与你的每一次互动中自我进化**的数字伙伴。它不仅帮你做事，更会在做事的过程中变得**越来越懂你、越来越强**。

### 1.2 市场定位

| 维度 | 定位 |
|------|------|
| **品类** | 个人 AI 操作系统级智能体 |
| **差异化锚点** | 持久记忆 + 自我进化 + 跨平台统一人格 |
| **技术层级** | 介于 LLM 应用层（ChatGPT 等）和系统层（OS Agent）之间 |
| **竞争对手** | Hermes Agent、OpenClaw、Claude Code、Devin、元宝、豆包 |

### 1.3 目标用户画像

| 用户画像 | 核心场景 | 痛点 |
|----------|----------|------|
| **独立开发者** | 全栈开发、项目管理、运维 | 需要记住项目上下文的助手，不想每次重新解释 |
| **技术管理者** | 代码审查、架构决策、多项目协调 | 需要跨项目/跨会话的记忆和决策辅助 |
| **数字原住民** | 个人知识管理、自动化生活、内容创作 | 需要统一的数字人格，不想管理多个 AI 工具 |
| **远程工作者** | 跨时区协作、信息聚合、任务调度 | 需要跨平台、异步运行的智能助手 |

### 1.4 市场规模预估

- **短期（1 年）**：面向技术型早期采用者，目标 10 万 DAU
- **中期（3 年）**：覆盖泛知识工作者，目标 100 万 DAU
- **长期（5 年）**：成为个人 AI 操作系统的标准层，目标 1000 万 DAU

---

## 二、核心要解决的痛点（对比现有工具的不足）

### 2.1 痛点评分矩阵

| 痛点 | 严重程度 | Hermes Agent | OpenClaw | Claude Code | EvoGen 目标 |
|------|----------|:---:|:---:|:---:|:---:|
| **无持久记忆** | 🔴 极高 | 部分解决（memory 工具） | 部分解决 | ❌ | ✅ 完全解决 |
| **不会自我进化** | 🔴 极高 | 部分解决（skills） | 部分解决（skills） | ❌ | ✅ 核心能力 |
| **跨会话上下文断裂** | 🟠 高 | 部分解决（session search） | 部分解决 | ❌ | ✅ 完全解决 |
| **多 Agent 协作混乱** | 🟠 高 | 较好（delegate_task + kanban） | 较好（specialist lanes） | ❌ | ✅ 进化型协作 |
| **重复性配置繁琐** | 🟡 中 | 较好（profiles） | 较好（workspace） | ❌ | ✅ 自适应 |
| **跨平台人格不一致** | 🟡 中 | 部分解决 | 部分解决 | N/A | ✅ 统一人格 |
| **工具生态碎片化** | 🟡 中 | 较好（MCP + 插件） | 较好（插件） | ❌ | ✅ 统一生态 |
| **学习成本高** | 🟡 中 | 中等 | 中等 | 低 | ✅ 渐进式引导 |

### 2.2 痛点详细分析

#### 痛点 1：缺乏持久记忆——"每次都是新大脑"

**现状**：Hermes Agent 的 memory 工具可以保存用户偏好和环境信息，但存储的是静态事实（key-value 语义）。OpenClaw 有类似机制。用户在跨会话时仍需大量重复解释项目上下文。

**EvoGen 的解决思路**：
- **三层记忆架构**：瞬态记忆（会话内）→ 工作记忆（跨会话项目上下文）→ 核心记忆（持久化自我认知）
- **自动记忆提取**：无需用户主动告知，Agent 在交互中自动识别并持久化关键信息
- **记忆衰减模型**：参考人类记忆机制，信息按重要性和使用频率自然衰减或强化

#### 痛点 2：不会自我进化——"工具永远停在 v1"

**现状**：Hermes Agent 的 skills 机制可以保存工作流程，但需要手动触发。OpenClaw 的 ClawHub 是静态技能市场。两者都不能从失败中学习、优化策略。

**EvoGen 的解决思路**：
- **经验回放机制**：每次任务的执行轨迹被记录和分析，失败自动触发反思和改进
- **策略版本化**：Agent 的提示词、工具选择策略、任务拆解策略随使用持续优化
- **A/B 自测试**：Agent 可以对自己的不同策略变体进行 A/B 测试，选择更优方案

#### 痛点 3：多 Agent 协作混乱——"左右手互搏"

**现状**：Hermes Agent 的 `delegate_task` 是同步的，父进程中断则子进程丢失。Kanban 是持久化的但需要手动调度。OpenClaw 的 specialist lanes 需要预配置路由规则。

**EvoGen 的解决思路**：
- **自主编排**：Agent 根据任务复杂度和类型自动决定是否拆解、如何拆解、分配给谁
- **上下文继承**：子 Agent 继承父 Agent 的相关记忆和上下文，而非从零开始
- **结果融合**：多 Agent 的结果自动合并、去重、冲突解决

---

## 三、功能需求列表（按优先级排序）

### P0 — 核心灵魂功能（必须首发）

| ID | 功能 | 说明 | 对标来源 |
|----|------|------|----------|
| F-001 | **进化记忆引擎** | 三层记忆（瞬态/工作/核心）+ 自动提取 + 衰减模型 | 自研（核心差异化） |
| F-002 | **经验学习环路** | 任务回放 → 反思 → 策略优化 → A/B 验证 | 自研（核心差异化） |
| F-003 | **统一人格系统** | 跨平台一致的行为风格、知识图谱、价值偏好 | 融合 Hermes + OpenClaw |
| F-004 | **多平台通道网关** | WebSocket 协议 + 平台适配器，支持 Telegram/Discord/Slack/飞书/微信等 | 融合 Hermes + OpenClaw |

### P1 — 核心能力（首发必须）

| ID | 功能 | 说明 | 对标来源 |
|----|------|------|----------|
| F-005 | **自适应任务拆解** | 根据任务复杂度自动决定拆解策略，子任务依赖图管理 | Hermes delegate_task + 自研 |
| F-006 | **多 Agent 进化协作** | 子 Agent 继承记忆，结果自动融合，失败自动重试 | Hermes kanban + 自研 |
| F-007 | **技能市场 + 自生成技能** | 社区技能 + Agent 自动从经验中提炼技能 | Hermes skills + OpenClaw ClawHub |
| F-008 | **工具链编排器** | 可视化 + 自然语言混合编排，MCP 原生支持 | Hermes MCP + OpenClaw 工具 |
| F-009 | **沙盒执行环境** | Docker/SSH/本地多后端，按任务危险等级自动选择 | OpenClaw 沙盒 + 自研 |
| F-010 | **上下文智能压缩** | 混合压缩：摘要 + 关键信息抽取 + 向量检索 | Hermes compression |

### P2 — 差异化亮点（首发或快速迭代）

| ID | 功能 | 说明 | 对标来源 |
|----|------|------|----------|
| F-011 | **主动意图预测** | 基于用户行为模式预测意图，提前准备上下文 | 自研 |
| F-012 | **进化的提示词** | 系统提示词随使用自动优化，减少 token 浪费 | 自研 |
| F-013 | **多模态融合理解** | 语音、图片、视频、代码统一理解管道 | OpenClaw Voice + 自研 |
| F-014 | **实时 Canvas 工作区** | Agent 可编辑的 HTML/CSS/JS 画布 | OpenClaw Canvas |
| F-015 | **Cron 进化调度** | 定时任务根据历史执行结果自动调整策略 | Hermes cron + 自研 |
| F-016 | **知识图谱构建** | 自动从交互中提取实体、关系，构建个人知识图谱 | 自研 |
| F-021 | **GUI Agent 网页操控** | 基于页面理解（VLM + DOM 解析）自动操作网页，支持人机协同（人在回路）完成复杂 Web 任务 | 自研（Playwright + 视觉模型） |

### P3 — 生态增强（后续迭代）

| ID | 功能 | 说明 |
|----|------|------|
| F-017 | **自然语言编程 IDE 插件** | VSCode/JetBrains 深度集成 |
| F-018 | **个人数据聚合器** | 邮件、日历、消息、文件统一索引 |
| F-019 | **语音全双工交互** | 支持打断、情绪感知、多人对话 |
| F-020 | **端侧轻量部署** | 支持树莓派、NAS 等边缘设备 |

---

## 四、非功能需求

### 4.1 性能需求

| 指标 | 目标 | 说明 |
|------|------|------|
| **首响应时间（TTFR）** | < 2 秒 | 用户发送消息到第一段流式回复 |
| **端到端延迟（P95）** | < 5 秒 | 包含工具调用，简单任务 |
| **记忆检索延迟** | < 500ms | 向量检索 + 图谱查询 |
| **并发会话数** | ≥ 100 | 单节点同时活跃会话 |
| **上下文窗口利用率** | > 70% | 有效信息占上下文比例 |
| **Token 消耗优化** | 比基线减少 30% | 通过进化的提示词压缩 |

### 4.2 安全需求

| 需求 | 说明 |
|------|------|
| **沙盒隔离** | 非主会话默认在 Docker 沙盒中执行，可配置白名单 |
| **命令审批** | 危险命令（rm -rf、git push --force 等）需用户确认 |
| **密钥不落盘** | API Key/Token 仅存在于环境变量或加密存储 |
| **DM 配对** | 未知用户 DM 需配对码验证 |
| **数据本地化** | 记忆和知识图谱数据默认存储在用户本地 |
| **审计日志** | 所有操作完整记录，支持回溯 |
| **端到端加密** | 远程访问时的通信加密 |

### 4.3 可用性需求

| 需求 | 说明 |
|------|------|
| **安装即用** | 一条命令完成安装和基础配置 |
| **渐进式引导** | 新手模式逐步解锁高级功能 |
| **自然语言配置** | 配置项支持自然语言修改 |
| **容错降级** | 模型不可用时自动切换备用模型 |
| **离线有限能力** | 本地模型支持部分离线场景 |

### 4.4 可扩展性需求

| 需求 | 说明 |
|------|------|
| **插件热加载** | 安装插件无需重启 |
| **MCP 协议兼容** | 完整支持 MCP 标准 |
| **多模型提供商** | 兼容 20+ 提供商 |
| **开放 API** | REST + WebSocket API 供第三方集成 |
| **配置即代码** | 支持 Git 版本化的配置文件 |

### 4.5 可靠性需求

| 需求 | 说明 |
|------|------|
| **Gateway 守护进程** | systemd/launchd 自动重启 |
| **记忆持久化** | SQLite + 向量数据库双重保障 |
| **会话恢复** | 中断后精确恢复上下文 |
| **优雅降级** | 依赖服务不可用时不崩溃 |
| **备份与迁移** | 一键备份/恢复全部个人数据 |

### 4.6 架构设计约束

| 约束 | 说明 |
|------|------|
| **BS 架构（瘦客户端）** | 网页前端仅负责交互展示和用户输入输出，不承载任何业务逻辑。所有状态管理、流程编排、数据校验统一在服务端完成。前端框架选型（React/Vue/Svelte）只影响渲染层 |
| **重服务端** | 核心计算（AI 推理链、任务调度引擎、记忆检索、知识图谱运算、策略优化）全部在服务端执行。前端为纯展示层，仅做 DOM 渲染与事件传递，浏览器端不执行任何模型推理 |
| **前后端分离** | 前端（Web UI）与后端（API Server）独立仓库、独立构建、独立部署、独立扩缩容。通信仅通过明确定义的 RESTful API + WebSocket 协议，前端不直接访问数据库、文件系统或模型服务 |
| **解耦式模块设计** | 后端内部各模块（记忆引擎、任务调度器、通道网关、技能市场、沙盒管理器等）通过 Interface/Protocol 抽象层通信，遵循依赖倒置原则（DIP）。模块可独立开发、独立测试、独立替换、独立演进。模块间禁止直接 import 内部实现 |

### 4.7 认证与权限系统

| 需求 | 说明 |
|------|------|
| **多用户认证** | 支持本地账号密码、OAuth 2.0（GitHub / Google / 飞书 / 企业微信）、LDAP / SAML 单点登录。所有认证方式通过统一认证中间件接入 |
| **角色权限控制（RBAC）** | 预定义角色（超级管理员 / 管理员 / 开发者 / 查看者）+ 支持自定义角色。细粒度权限控制到功能操作级别（如：创建会话、管理技能、查看审计日志、修改系统配置） |
| **Token 会话管理** | 基于 JWT（Access Token + Refresh Token）的无状态会话管理，支持 Token 过期、刷新、主动吊销。API 级别访问控制：每个端点按需鉴权 |
| **操作审计** | 所有用户操作完整记录（操作人、操作时间、操作类型、目标资源、执行结果），审计日志不可篡改，支持按时间范围、操作人、操作类型查询与导出 |
| **数据隔离** | 用户间记忆、技能、会话、配置数据通过租户 ID + 用户 ID 双重逻辑隔离。支持数据库级别物理隔离（独立 Database/Schema）或行级隔离，根据部署规模灵活选择 |

### 4.8 企业级特性

| 特性 | 说明 |
|------|------|
| **高可用（HA）** | 无状态服务设计支持水平扩展（多节点部署），数据库主从复制 + 自动故障转移。关键路径（API 网关、调度器）冗余部署。目标可用性 ≥ 99.9%（单可用区）/ ≥ 99.99%（多可用区） |
| **可监控** | 集成 Prometheus metrics 暴露 + Grafana 仪表盘预置。OpenTelemetry 分布式链路追踪（Trace），结构化日志（JSON 格式，按 request_id 串联）。健康检查端点（/healthz、/readyz）供 K8s 探针使用 |
| **可审计** | 全量操作审计日志持久化存储（不可篡改设计），敏感操作（删除数据、修改权限、API Key 变更）二次确认。支持 SOC 2 / ISO 27001 合规报告导出 |
| **多租户** | 租户级资源配额（最大会话数、Token 消耗上限、存储上限、并发数），租户级速率限制。租户管理员可自定义部分 UI 品牌（Logo、配色、欢迎页）与默认策略 |
| **API 协议兼容** | 完整兼容 OpenAI API 格式（`/v1/chat/completions`、`/v1/embeddings` 等）和 Anthropic Messages API，可作为 Drop-in Replacement 接入已适配这些协议的第三方工具和客户端 |
| **部署灵活** | 支持单机 Docker Compose 快速部署（个人/小团队）、Kubernetes Helm Chart 集群部署（中大型企业）、私有化离线部署（高安全需求场景） |

### 4.9 可扩展性增强（补充 4.4）

| 增强项 | 说明 |
|------|------|
| **插件化架构** | 核心功能通过插件接口暴露（Plugin SDK），第三方可开发自定义插件。插件支持热加载（动态发现 + 生命周期管理），插件间通过事件总线通信，互不耦合。插件市场支持版本管理与依赖声明 |
| **多协议网关** | 除 OpenAI / Anthropic 协议外，支持 gRPC（高性能内部调用）、GraphQL（灵活查询）等协议接入，通过统一协议适配层转换，后端模块对协议层透明 |

---

## 五、成功指标

### 5.1 北极星指标

> **"进化有效性"**：用户使用 EvoGen 一个月后，完成相同任务所需的平均交互轮次比初始减少 ≥ 40%

### 5.2 关键指标

| 指标 | 目标（3 个月） | 目标（12 个月） |
|------|:---:|:---:|
| 日活用户 (DAU) | 5,000 | 50,000 |
| 次日留存 | > 60% | > 70% |
| 周留存 | > 40% | > 55% |
| 平均会话时长 | > 15 分钟 | > 25 分钟 |
| 技能自生成率 | > 5% 用户 | > 30% 用户 |
| NPS | > 40 | > 55 |

---

## 六、项目里程碑

| 阶段 | 时间 | 目标 |
|------|------|------|
| **M0 - 原型验证** | 第 1-2 月 | 三层记忆 + 基础任务执行可用 |
| **M1 - Alpha** | 第 3-4 月 | 经验学习环路闭环，多平台通道可用 |
| **M2 - Beta** | 第 5-6 月 | 多 Agent 协作、技能市场上线 |
| **M3 - 1.0 发布** | 第 7-8 月 | 完整 P0+P1 功能，开源发布 |
| **M4 - 生态构建** | 第 9-12 月 | 插件生态、端侧部署、开发者社区 |

---

## 七、附录：名词定义

| 术语 | 定义 |
|------|------|
| **进化记忆** | 三层架构的记忆系统，自动提取、衰减和强化 |
| **经验回放** | 重放历史任务执行轨迹，分析改进点 |
| **策略版本化** | Agent 的行为策略（提示词、工具选择等）随版本管理 |
| **统一人格** | 跨平台、跨会话一致的行为模式和价值偏好 |
| **通道网关** | 统一的消息平台接入层，WebSocket 协议 |
| **技能自生成** | Agent 从成功/失败经验中自动提炼可复用的工作流 |
